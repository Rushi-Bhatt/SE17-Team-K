<% provide(:title, @user.name) %>
<div class="col-md-8">
  <% if @user.reviews.any? %>
      <h3>Microposts (<%= @user.reviews.count %>)</h3>
      <ol class="microposts">
        <%= render @reviews %>
      </ol>
      <%= will_paginate @reviews %>
  <% end %>
</div>